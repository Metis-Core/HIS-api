export enum AuthProvider {
  USERNAME_PASSWORD = 'USERNAME_PASSWORD',
  //AZURE_AD = 'AZURE_AD', // This one i envision us using microsoft or organization accounts just like we have in total.  
  //GOOGLE = 'GOOGLE',
}